apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.reviews.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.reviews.port }}
    targetPort: 9080
  selector:
    app: {{ .Release.Name }}